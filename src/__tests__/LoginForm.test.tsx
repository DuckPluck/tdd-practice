import {
  render, // Рендерит компоненты и возвращает вспомогательные методы
  fireEvent, // Симулирует DOM events
  waitForElement, // Ожидает изменения в пользовательском интерфейсе
} from "@testing-library/react";

describe('<LoginForm />', () => {
  test("should display a blank login form, with remember me checked by default", async () => {

  });
});
